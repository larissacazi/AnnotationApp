<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/fundo"
    tools:context="zimmermann.larissa.annotationapp.MainActivity">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="21dp"
        app:srcCompat="@drawable/logo" />

    <EditText
        android:id="@+id/textID"
        android:layout_width="320dp"
        android:layout_height="360dp"
        android:layout_alignLeft="@+id/imageView"
        android:layout_alignStart="@+id/imageView"
        android:layout_below="@+id/imageView"
        android:background="@drawable/fundo_anotacao"
        android:elevation="1dp"
        android:ems="10"
        android:gravity="start"
        android:inputType="textPersonName"
        android:paddingBottom="5dp"
        android:paddingLeft="35dp"
        android:paddingRight="5dp"
        android:paddingTop="5dp"
        android:textColor="@color/brownChoosen"
        android:textSize="@dimen/textSizeAnnotation" />

    <ImageView
        android:id="@+id/saveButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/textID"
        android:layout_alignStart="@+id/textID"
        android:layout_below="@+id/textID"
        app:srcCompat="@drawable/botao_salvar" />
</RelativeLayout>
